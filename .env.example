# Copiez vers .env et renseignez vos identifiants Migros Online.
# Ne commitez jamais .env

MIGROS_EMAIL=vous@exemple.com
MIGROS_PASSWORD=votre-mot-de-passe
# Seed TOTP base32 (si 2FA activée) — pas le code à 6 chiffres
MIGROS_TOTP_SECRET=

# Cible du push et export promos — uv run python validate.py lists
# Si les listes ne correspondent pas au site : validate.py logout puis relancer lists
MIGROS_SHOPPING_LIST_ID=
MIGROS_SHOPPING_LIST_NAME=S1

# Slug du lien partagé (optionnel, ≠ shoppingListId)
# MIGROS_LIST_SLUG=

# true = export promos sans filtre fruit/pain/yaourt (liste S1 déjà curatée sur Migros)
MIGROS_PROMOS_NO_FILTER=true

# Minimum livraison Migros Online pour l'estimation budget (défaut : 99)
MIGROS_MIN_DELIVERY_CHF=99

# Telegram — partage dans le topic Comida du groupe coloc
# 1. Bot via @BotFather  2. validate.py telegram-setup pour récupérer les ids
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
TELEGRAM_TOPIC_ID=
# Envoi auto après promos (sans --telegram) et/ou après push
TELEGRAM_AUTO_SEND=false
TELEGRAM_SEND_ON_PUSH=false
